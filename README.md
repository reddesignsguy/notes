# notes for myself but made public so i can show off lol

_**cmake**_
- add_subdirectory(subdir) -- used to reference another cmake project, where the subdir contains the CMakeLists.txt associated with said project
- find_package() -- looks for ".cmake" files
  - vcpkg used as a "cmake toolchain" to help find packages?
